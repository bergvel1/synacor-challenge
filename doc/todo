TODO

- correct files paths for IO to reflect new folder structure

make VM struct
	- contains regs, mem, stack, PC

make "inst.c"
	- contains helper functions for each opcode
		- possibly with a flag for executing vs. logging
